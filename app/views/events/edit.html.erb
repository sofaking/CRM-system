<div id='edit_event' class='paper'>
  <div class='path'>
    <%= link_to 'Projects', current_user.account %> >
    <%= link_to @event.project.name, account_project_path(current_user.account, @event.project) if @event.project %> >
    <%= link_to @event.title, '' %>
  </div>
  <h1>Event</h1>

  <%= render 'form' %>
  
  <%= link_to 'Back', :back %>
</div>