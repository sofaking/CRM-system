<div class="form_project paper">
  <div class='path'>
    <%= link_to 'Projects', current_user.account %> >
    <%= link_to @project.name, '' %>
  </div>
  <%= form_for @project, url: account_project_path(@project.account, @project), remote: true do |f| %>
    <% if @project.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

        <ul>
        <% @project.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.text_field :name %>
      <span id='actions'>
        <%= f.submit value: ' Save ' %> or <%= link_to 'Cancel', '' %>
        <%= link_to 'Destroy', account_project_path(@project.account, @project), method: :delete, data: { confirm: 'Are you sure?' }%>
      </span>
      <br/>
      <%= f.text_field :description %>
      <span id='actions'>
        <%= f.submit value: ' Save ' %> or <%= link_to 'Cancel', '' %>
        <%= link_to 'Destroy', account_project_path(@project.account, @project), method: :delete, data: { confirm: 'Are you sure?' }%>
      </span>
    </div>
  <% end %>
  <div id='project_menu'>
    <%= link_to "#{@project.todo_lists.count} Todo lists", account_project_todo_lists_path(@project.account, @project) %>
  </div>
  <h1 id='todo_label'>To-do lists</h1>
  <ul class="todo_lists" id=<%= "project_#{@project.id}" %> >
    <%= render @project.todo_lists %>
  </ul>
  <%= link_to "add todo list", account_project_todo_lists_path(todo_list: {name: 'New todo list', project_id: @project.id}, commit: 'Create todo list', account_id: @project.account.id, project_id: @project.id), method: :post, remote: true, id: :add_todo_list %>
  <h1 id='event'>Events</h1>
  <ul class="events" id=<%= "project_#{@project.id}" %> >
    <%= render @project.events %>
  </ul>
  <%= link_to "add event", account_project_events_path(event: {name: 'New event', project_id: @project.id}, commit: 'Create event', account_id: @project.account.id, project_id: @project.id), method: :post, remote: true, id: :add_event %>
</div>