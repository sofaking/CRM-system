<%= form_for @project, url: [current_user.account, @project], remote: true do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name, placeholder: 'Project name' %>
    <span id='actions'>
      <%= f.submit value: ' Save ' %> or <%= link_to 'Cancel', '' %>
      <%= link_to 'Destroy', account_project_path(current_user.account, @project), method: :delete, data: { confirm: 'Are you sure?' }%>
    </span>
    <br/>
    <%= f.text_field :description, placeholder: 'Project description' %>
    <span id='actions'>
      <%= f.submit value: ' Save ' %> or <%= link_to 'Cancel', '' %>
      <%= link_to 'Destroy', account_project_path(current_user.account, @project), method: :delete, data: { confirm: 'Are you sure?' }%>
    </span>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
